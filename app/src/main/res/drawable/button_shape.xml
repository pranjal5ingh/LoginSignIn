<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/Deep_Green_Cyan" />

    <!-- Corner radius -->
    <corners android:radius="16dp" />

    <!-- Optional: Border -->
    <stroke
        android:width="2dp"
        android:color="#004643" />
</shape>
